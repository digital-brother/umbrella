name: Test

on:
  push:
    branches: [ develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Build compose containers
      run: |
        cp .envs/.env.local.example .envs/.env.local
        docker-compose -f local.yml build
    - name: Run pytest
      run: docker-compose -f local.yml run web pytest
    - name: Run flake8
      run: docker-compose -f local.yml run flake8
